{
  "name": "@wallet/smoke",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test coolify-smoke.test.ts",
    "build": "bun run typecheck",
    "typecheck": "tsc --noEmit --allowJs false --moduleResolution Bundler --module ESNext --target ES2022 --types bun coolify-smoke.test.ts"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
